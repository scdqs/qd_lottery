# 🔧 故障排除指南

## 传感器相关问题

### ❌ 问题：显示"传感器不可用"（Android设备）

**原因：**
现代浏览器（Chrome、Firefox等）要求访问传感器的网站必须使用HTTPS协议或localhost地址。由于我们使用的是局域网IP地址（http://），浏览器会阻止传感器访问。

**解决方案：**
1. **使用"按钮模式"**（推荐）⭐
   - 点击页面上的"使用按钮模式"按钮
   - 抽奖开始后，点击"点击摇一摇 +1"按钮
   - 功能完全相同，只是操作方式不同

2. 使用localhost访问（仅限电脑测试）
   - 在电脑浏览器访问 http://localhost:3002
   - 传感器可以正常工作

3. 配置HTTPS（生产环境）
   - 申请SSL证书
   - 配置HTTPS服务器
   - 使用域名访问

### ❌ 问题：显示"设备不支持"

**可能原因：**
1. 设备确实不支持加速度传感器（极少见）
2. 浏览器不支持 DeviceMotion API
3. 使用了桌面浏览器而非移动浏览器

**解决方案：**
- ✅ 确保使用真实的移动设备（手机或平板）
- ✅ 使用现代浏览器（Safari、Chrome、Firefox）
- ✅ 更新浏览器到最新版本
- ✅ 不要使用桌面浏览器的移动模拟模式

### ❌ 问题：显示"需要授权"（iOS设备）

**原因：**
iOS 13+ 要求用户明确授权才能访问运动传感器

**解决方案：**
1. 点击页面上的"授权访问传感器"按钮
2. 在弹出的系统对话框中选择"允许"
3. 如果没有弹窗，检查Safari设置：
   - 设置 → Safari → 隐私与安全性
   - 确保"运动与方向访问"已开启

### ❌ 问题：授权后仍然不工作

**解决方案：**
1. **刷新页面**重新加载
2. 清除浏览器缓存和Cookie
3. 重启Safari浏览器
4. 检查iOS系统设置：
   - 设置 → 隐私 → 运动与健身
   - 确保Safari已开启

### ❌ 问题：摇动没有反应

**可能原因：**
1. 摇动力度不够
2. 传感器未正确初始化
3. 抽奖还未开始

**解决方案：**
- ✅ 更用力地摇动手机
- ✅ 确保电脑端已点击"开始抽奖"
- ✅ 检查页面是否显示"开始摇一摇！"
- ✅ 刷新页面重新加入

## 网络连接问题

### ❌ 问题：无法访问页面

**解决方案：**
1. 确保手机和电脑在**同一WiFi网络**
2. 检查IP地址是否正确（当前：192.168.68.56）
3. 检查电脑防火墙设置
4. 尝试在电脑浏览器访问测试

### ❌ 问题：连接断开

**解决方案：**
1. 检查WiFi信号强度
2. 刷新页面重新连接
3. 重启后端服务器
4. 检查后端日志是否有错误

## 授权相关问题

### ❌ 问题：显示"appid为空"

**原因：**
尝试使用微信授权但未配置AppID

**解决方案：**
- ✅ 使用"测试模式"代替微信授权
- ✅ 点击"使用测试模式"按钮
- ✅ 输入昵称即可参与

### ❌ 问题：测试模式无法加入

**解决方案：**
1. 确保输入了昵称
2. 检查会话ID是否正确
3. 查看浏览器控制台是否有错误
4. 刷新页面重试

## 浏览器兼容性

### ✅ 推荐浏览器

**iOS：**
- Safari（推荐）
- Chrome for iOS

**Android：**
- Chrome（推荐）
- Firefox
- Samsung Internet

### ❌ 不支持的浏览器

- 微信内置浏览器（可能需要额外配置）
- 旧版本浏览器（iOS 12及以下）
- 桌面浏览器的移动模拟模式

## 常见错误信息

### "Motion sensor permission denied"
**含义：**用户拒绝了传感器权限
**解决：**重新授权或在设置中开启权限

### "Device does not support motion sensors"
**含义：**设备不支持或浏览器不支持
**解决：**更换设备或浏览器

### "连接服务器失败"
**含义：**无法连接到后端服务器
**解决：**检查网络连接和服务器状态

### "会话ID不存在"
**含义：**输入的会话ID无效或已过期
**解决：**重新获取会话ID

## 调试技巧

### 查看浏览器控制台

**iOS Safari：**
1. 在Mac上打开Safari
2. 开发 → 连接iPhone → 选择页面
3. 查看控制台错误信息

**Android Chrome：**
1. 在电脑Chrome中访问 `chrome://inspect`
2. 连接手机并启用USB调试
3. 点击"inspect"查看控制台

### 测试传感器

在浏览器控制台输入：
```javascript
// 测试传感器是否可用
console.log('DeviceMotionEvent:', typeof DeviceMotionEvent);

// 测试权限API
console.log('requestPermission:', typeof DeviceMotionEvent.requestPermission);

// 监听传感器事件
window.addEventListener('devicemotion', (e) => {
  console.log('Acceleration:', e.accelerationIncludingGravity);
});
```

## 仍然无法解决？

1. 查看后端日志：
   ```bash
   # 查看后端进程输出
   # 检查是否有错误信息
   ```

2. 检查服务器状态：
   - 后端：http://192.168.68.56:3000
   - Web端：http://192.168.68.56:3001
   - H5端：http://192.168.68.56:3002

3. 重启所有服务：
   ```bash
   # 停止所有服务
   # 重新启动
   cd backend && npm run dev
   cd web-client && npm run dev
   cd h5-client && npm run dev
   ```

4. 提供以下信息寻求帮助：
   - 设备型号和系统版本
   - 浏览器名称和版本
   - 错误信息截图
   - 浏览器控制台日志

## 💡 最佳实践

- ✅ 使用最新版本的iOS或Android系统
- ✅ 使用Safari（iOS）或Chrome（Android）浏览器
- ✅ 确保WiFi信号良好
- ✅ 在测试环境使用"测试模式"
- ✅ 摇动时保持手机竖直握持
- ✅ 用力且快速地摇动手机
